// Placeholder — replaced by Preact bundle in Phase 7.
// If you see this in a storefront, the widget hasn't been built yet.
(function () {
	const containers = document.querySelectorAll(".countdown-timer-root");
	containers.forEach((el) => {
		el.innerHTML =
			'<div style="padding:8px;border:1px dashed #999;color:#666;font-size:12px;text-align:center;">Countdown Timer block — widget bundle pending (Phase 7).</div>';
	});
})();
